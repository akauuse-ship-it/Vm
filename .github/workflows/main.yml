name: Moonlight Windows VM

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 355

    steps:
      - name: Download and install components
        shell: powershell
        run: |
          $ProgressPreference = 'SilentlyContinue'
          Write-Host "Downloading Tailscale..."
          curl.exe -L -o tailscale-setup.exe "https://pkgs.tailscale.com/stable/tailscale-setup-latest.exe"
          Start-Process -FilePath ".\tailscale-setup.exe" -ArgumentList "/quiet" -Wait
          Write-Host "Downloading Sunshine..."
          curl.exe -L -o sunshine.zip "https://github.com/LizardByte/Sunshine/releases/download/v0.23.1/sunshine-windows-portable.zip"
          if ((Get-Item sunshine.zip).Length -lt 100KB) { Write-Host "ERROR: Sunshine download failed"; exit 1 }
          Expand-Archive sunshine.zip -DestinationPath C:\Sunshine -Force
          Write-Host "Installing ViGEmBus..."
          curl.exe -L -o vigembus.exe "https://github.com/nefarius/ViGEmBus/releases/download/v1.22.0/ViGEmBus_1.22.0_x64_x86_arm64.exe"
          Start-Process -FilePath ".\vigembus.exe" -ArgumentList "/qn" -Wait
          Write-Host "Components installed."

      - name: Install Virtual Display
        shell: powershell
        run: |
          $ProgressPreference = 'SilentlyContinue'
          curl.exe -L -o usbmmidd.zip "https://www.amyuni.com/downloads/usbmmidd_v2.zip"
          if ((Get-Item usbmmidd.zip).Length -lt 100KB) { Write-Host "WARNING: skip virtual display"; exit 0 }
          Expand-Archive usbmmidd.zip -DestinationPath C:\usbmmidd -Force
          $dir = Get-ChildItem -Path C:\usbmmidd -Recurse -Filter "deviceinstaller64.exe" | Select-Object -First 1 | ForEach-Object { $_.DirectoryName }
          Set-Location $dir
          .\deviceinstaller64.exe install usbmmidd.inf usbmmidd
          Start-Sleep -Seconds 5
          .\deviceinstaller64.exe enableidd 1
          Start-Sleep -Seconds 8
          Write-Host "Virtual display enabled."

      - name: Enable Remote Desktop
        shell: powershell
        run: |
          net user runneradmin "RdpPass123!"
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server" -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" -Name "UserAuthentication" -Value 0
          Write-Host "RDP enabled."

      - name: Configure Sunshine
        shell: powershell
        run: |
          $sunshineDir = Get-ChildItem -Path C:\Sunshine -Recurse -Filter "sunshine.exe" | Select-Object -First 1 | ForEach-Object { $_.DirectoryName }
          Write-Host "Sunshine at: $sunshineDir"
          echo "SUNSHINE_DIR=$sunshineDir" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          $confDir = Join-Path $sunshineDir "config"
          New-Item -ItemType Directory -Path $confDir -Force | Out-Null
          Set-Content -Path (Join-Path $confDir "sunshine.conf") -Value "encoder = software"
          Add-Content -Path (Join-Path $confDir "sunshine.conf") -Value "sw_preset = ultrafast"
          Add-Content -Path (Join-Path $confDir "sunshine.conf") -Value "min_log_level = info"
          & (Join-Path $sunshineDir "sunshine.exe") --creds admin "ChangeMe123!"
          Write-Host "Config done."

      - name: Start Tailscale and Sunshine
        shell: powershell
        env:
          TS_AUTHKEY: ${{ secrets.TS_AUTHKEY }}
        run: |
          $ts = "C:\Program Files\Tailscale\tailscale.exe"
          & $ts up --authkey=$env:TS_AUTHKEY --hostname=moonlight-vm --accept-routes --unattended
          Start-Sleep -Seconds 5
          $tsIP = (& $ts ip -4).Trim()
          $status = & $ts status --json | ConvertFrom-Json
          $dnsName = $status.Self.DNSName.TrimEnd('.')
          Start-Process -FilePath (Join-Path $env:SUNSHINE_DIR "sunshine.exe") -WorkingDirectory $env:SUNSHINE_DIR
          Start-Sleep -Seconds 30
          & $ts serve --bg --https=47990 https+insecure://localhost:47990
          Write-Host "=========================================="
          Write-Host "  TAILSCALE IP : $tsIP"
          Write-Host "  WEB UI       : https://${dnsName}:47990"
          Write-Host "  WEB UI LOGIN : admin / ChangeMe123!"
          Write-Host "  RDP ADDRESS  : $tsIP  (port 3389)"
          Write-Host "  RDP LOGIN    : runneradmin / RdpPass123!"
          Write-Host "=========================================="

      - name: Keep alive
        shell: powershell
        run: |
          $cutoff = [DateTime]::Now.AddHours(5).AddMinutes(55)
          while ([DateTime]::Now -lt $cutoff) {
            $proc = Get-Process sunshine -ErrorAction SilentlyContinue
            if (-not $proc) {
              Write-Host "Sunshine down - restarting..."
              Start-Process -FilePath (Join-Path $env:SUNSHINE_DIR "sunshine.exe") -WorkingDirectory $env:SUNSHINE_DIR
              Start-Sleep -Seconds 20
            }
            Start-Sleep -Seconds 30
          }
          Write-Host "Done."
