name: Moonlight Windows VM
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360
    steps:
      # ── STEP 0: OS Tweaks, Audio & Virtual Display ──────────────────────
      - name: 0. OS Tweaks, Audio & Virtual Monitor
        shell: pwsh
        run: |
          powercfg /setactive 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c
          reg add "HKEY_CURRENT_USER\Control Panel\Desktop" /v UserPreferencesMask /t REG_BINARY /d 9012038010000000 /f
          reg add "HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System" /v DisableLockWorkstation /t REG_DWORD /d 1 /f
          reg add "HKLM\SOFTWARE\Policies\Microsoft\Windows\Personalization" /v NoLockScreen /t REG_DWORD /d 1 /f
          
          # Enable Windows Audio Service
          Write-Host "Enabling Windows Audio Service..."
          Set-Service -Name Audiosrv -StartupType Automatic
          Start-Service -Name Audiosrv -ErrorAction SilentlyContinue
          
          # Install Virtual Display Driver to fake a monitor
          Write-Host "Installing Virtual Display (usbmmidd)..."
          curl.exe --retry 5 -L -o usbmmidd.zip https://www.amyuni.com/downloads/usbmmidd_v2.zip
          Expand-Archive -Path usbmmidd.zip -DestinationPath C:\usbmmidd -Force
          
          # Dynamically find the exact path of the extracted installer
          $installer = Get-ChildItem -Path "C:\usbmmidd" -Filter "deviceinstaller64.exe" -Recurse | Select-Object -First 1 -ExpandProperty FullName
          $installerDir = Split-Path $installer
          
          # Run the installer from its specific folder
          Start-Process -FilePath $installer -WorkingDirectory $installerDir -ArgumentList "install usbmmidd.inf usbmmidd" -Wait
          Start-Process -FilePath $installer -WorkingDirectory $installerDir -ArgumentList "enableidd 1" -Wait
          
          # Install Visual C++
          curl.exe --retry 5 --retry-connrefused -L -o vc_redist.x64.exe https://aka.ms/vs/17/release/vc_redist.x64.exe
          Start-Process ./vc_redist.x64.exe -ArgumentList "/install /quiet /norestart" -Wait

      # ── STEP 1: Install Tailscale & Sunshine ──────────────────────
      - name: 1. Install Tailscale & Sunshine
        shell: pwsh
        run: |
          Write-Host "Downloading Tailscale..."
          curl.exe --retry 5 -L -o tailscale.exe https://pkgs.tailscale.com/stable/tailscale-setup-latest.exe
          Start-Process ./tailscale.exe -ArgumentList "/quiet" -Wait
          
          Write-Host "Downloading Sunshine..."
          $release = Invoke-RestMethod "https://api.github.com/repos/LizardByte/Sunshine/releases/latest"
          $asset = $release.assets | Where-Object { $_.name -like "*Windows-AMD64-portable.zip" } | Select-Object -First 1
          
          if (-not $asset) {
            Write-Error "Failed to find the portable ZIP in the latest release."
            exit 1
          }
          
          curl.exe --retry 5 -L -o sunshine.zip $($asset.browser_download_url)
          
          Write-Host "Extracting Sunshine..."
          Expand-Archive -Path sunshine.zip -DestinationPath "C:\Sunshine" -Force

      # ── STEP 2: Connect Tailscale, Disable Firewall & Start Sunshine ──────
      - name: 2. Connect Tailscale & Start Sunshine
        shell: pwsh
        env:
          RUN_NUMBER: ${{ github.run_number }}
        run: |
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey="tskey-auth-kvVxmawkXC11CNTRL-AMNCuL51xTPMaVM4AFLXUPAc5KYYqX72"
          
          Set-NetFirewallProfile -Profile Domain,Public,Private -Enabled False
          
          $sunshineExe = Get-ChildItem -Path "C:\Sunshine" -Filter "sunshine.exe" -Recurse | Select-Object -First 1 -ExpandProperty FullName
          Start-Process $sunshineExe -RedirectStandardError "C:\Sunshine\error_log.txt" -RedirectStandardOutput "C:\Sunshine\output_log.txt"
          
          Write-Host "Waiting 30 seconds for Sunshine to initialize..."
          Start-Sleep -Seconds 30
          $tailscaleIP = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
          
          Write-Host "=========================================="
          Write-Host "  RUN NUMBER       : $env:RUN_NUMBER"
          Write-Host "  TAILSCALE IP     : $tailscaleIP"
          Write-Host "  SUNSHINE WEB UI  : https://$tailscaleIP`:47990"
          Write-Host "=========================================="

          Write-Host "--- INTERNAL WEB SERVER TEST ---"
          try {
              $response = Invoke-WebRequest -Uri "https://localhost:47990" -SkipCertificateCheck -UseBasicParsing -ErrorAction Stop
              Write-Host "SUCCESS: The VM can access its own Sunshine server! Your Web UI should now be online."
          } catch {
              Write-Host "FAILED: The VM cannot reach Sunshine. Sunshine has crashed."
              Write-Host "ERROR: $($_.Exception.Message)"
          }

      # ── STEP 3: Keep Alive ──────
      - name: 3. Keep Alive
        shell: pwsh
        run: |
          $cutoff  = [DateTime]::Now.AddHours(5).AddMinutes(55)
          while ([DateTime]::Now -lt $cutoff) {
            Start-Sleep -Seconds 60
          }
